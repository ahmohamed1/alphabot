controller_manager:
  ros__parameters:
    update_rate: 10 # Hz

    alphabot_controller:
      type: diff_drive_controller/DiffDriveController

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    imu_broadcaster:
      type: imu_sensor_broadcaster/IMUSensorBroadcaster

# NOTE: The ros2_control controller instance keeps the name "alphabot_controller"
# (not "servicebot_controller") on purpose: this is the generic controller_manager
# instance name used across every robot model, matching the joint_state_broadcaster
# spawner call in alphabot_controller/launch/controller.launch.py and the
# /alphabot_controller/* topic names consumed by twist_mux, twist_relay and the
# localization stack. Only the physical parameters below differ per model.
alphabot_controller:
  ros__parameters:
    type        : diff_drive_controller/DiffDriveController
    use_stamped_vel: false
    left_wheel_names  : ['wheel_left_joint']
    right_wheel_names : ['wheel_right_joint']

    publish_rate: 10.0               # default: 50
    pose_covariance_diagonal : [0.001, 0.001, 1.0e-3, 1.0e-3, 1.0e-3, 0.01]
    twist_covariance_diagonal: [0.001, 0.001, 1.0e-3, 1.0e-3, 1.0e-3, 0.01]

    # TODO: PLACEHOLDER GEOMETRY. servicebot's wheel separation/radius were not
    # measurable from the CAD-exported xacro (the true offsets are baked into
    # the STL meshes, not the joint origins). Calibrate against the real/CAD
    # model before trusting odometry, and update these two values.
    wheel_separation : 0.23
    wheel_radius : 0.075

    wheel_separation_multiplier: 1.0
    left_wheel_radius_multiplier: 1.0
    right_wheel_radius_multiplier: 1.0

    # Velocity commands timeout [s], default 0.5
    cmd_vel_timeout: 0.5

    # Base frame_id
    base_frame_id: base_footprint #default: base_link

    # Publish limited velocity
    publish_limited_velocity: true

    # Publish wheel data
    publish_wheel_data: true

    # Publish to TF directly or not
    enable_odom_tf: true

    # Velocity and acceleration limits
    # Whenever a min_* is unspecified, default to -max_*
    # TODO: PLACEHOLDER LIMITS - servicebot is heavier (~16.5kg vs ~2kg for
    # alphabot) so these are kept conservative; retune once real hardware
    # limits are known.
    linear:
      x:
        has_velocity_limits    : true
        max_velocity           : 0.4  # m/s
        min_velocity           : -0.2 # m/s
        has_acceleration_limits: true
        max_acceleration       : 0.8  # m/s^2
        min_acceleration       : -0.4 # m/s^2
        has_jerk_limits        : false
        max_jerk               : 5.0  # m/s^3
    angular:
      z:
        has_velocity_limits    : true
        max_velocity           : 1.5 # rad/s
        has_acceleration_limits: true
        max_acceleration       : 2.0 # rad/s^2
        has_jerk_limits        : false
        max_jerk               : 2.5  # rad/s^3
